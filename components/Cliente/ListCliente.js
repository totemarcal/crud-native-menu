
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

function ListCliente({}) {
  return(
    <View>
        <Text>Lista Cliente</Text>
    </View>
  )
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });

  export default ListCliente
  